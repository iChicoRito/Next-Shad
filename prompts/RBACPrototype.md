Objective #1:

    1). (Already Implemented)
        Here in my Role-Based Access Control page, since im just prototyping, no backend and database yet, i want to change
        the static data here and contents.

    2). (Already Implemented)
        Remember, static and for prototyping purposes only.

    3). (Already Implemented)
        In the Add modal, make it Add New Roles, the form fields must contains that is related to the roles which are these:

        - Role Name
        - Description
        - Status

    4). (Already Implemented)
        In the datatables, make it displays all the fields for that roles along with their filtrations.

    5). (Already Implemented)
        Rename the file user-form-dialog to role-form-dialog. this is the only file you need to rename.

    6). (New)
        In the table, remove the "Permissions", "Last Modified" and the "User Assigned" columns.

Objective #2:

    1). Make this for Permissions, rename the role-form into permission-form.

    2). The data-table should shows these columns for permissions:

        - Permission Name
        - Description
        - Status

    3). In the datatables, make it displays all the fields for that permissions along with filtrations.

    4). Ensure u must follow the columns and the contents of the form modal.

Objective #3:

    1). Here in the roles, in the action button, add menu for View Details

    2). When click, it shows an dialog that shows the details of that selected rows.

    3). Provide only the code that need a change do not re-write the whole code.